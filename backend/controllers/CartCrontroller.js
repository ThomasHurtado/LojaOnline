const Cart = require('../models/Cart')


module.exports = class ProductController{

    static async create(req,res){

        const car = new Cart({
            owner: {
                _id: _id
            }
        })

        
    }
}